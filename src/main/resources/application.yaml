server:
  port: 8090
  servlet:
    context-path: /sample

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG

spring:
  application:
    name: sample-auth-server

oauth2:
  client-id: web-client
  client-secret: web-secret
  token-uri: http://localhost:8090/sample/oauth2/token
  redirect-uri: http://localhost:8090/sample/login/oauth2/code/demo-client

